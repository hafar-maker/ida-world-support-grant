import { redirect } from 'next/navigation'
import Link from 'next/link'
import { DashboardShell } from '@/components/dashboard-shell'
import { createClient } from '@/lib/supabase/server'
export default async function DocumentsPage(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login?next=/dashboard/documents');const {data:app}=await supabase.from('applications').select('application_number,status').eq('applicant_id',user.id).order('created_at',{ascending:false}).limit(1).maybeSingle();return <DashboardShell><div className="p-5 lg:p-8"><Link href="/dashboard" className="text-xs font-bold text-[#005EA8]">← Dashboard</Link><h1 className="mt-4 text-2xl font-extrabold text-[#12304A]">Documents</h1><div className="mt-6 max-w-3xl rounded-xl border bg-white p-7"><h2 className="font-bold">Application documents</h2>{app?<><p className="mt-2 text-sm text-slate-500">Application {app.application_number}. Documents requested by the grant team will appear here.</p><div className="mt-6 rounded-lg border border-dashed border-[#b9cbd5] bg-[#f8fafb] p-8 text-center"><p className="text-sm font-semibold text-[#12304A]">No documents requested</p><p className="mt-2 text-xs text-slate-500">You will be notified in Messages if additional documents are required.</p></div></>:<p className="mt-2 text-sm text-slate-500">Start an application before uploading or responding to document requests.</p>}</div></div></DashboardShell>}
