'use client'
import { FormEvent, useEffect, useState } from 'react'
import Link from 'next/link'
export default function AdminGrants(){const [grants,setGrants]=useState<any[]>([]);const [title,setTitle]=useState('');const [description,setDescription]=useState('');const [category,setCategory]=useState('General');const [error,setError]=useState('');async function load(){const r=await fetch('/api/grants');if(r.ok)setGrants(await r.json())}useEffect(()=>{load()},[]);async function add(e:FormEvent){e.preventDefault();setError('');const r=await fetch('/api/grants',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title,description,category,status:'open'})});const d=await r.json();if(!r.ok){setError(d.error);return}setTitle('');setDescription('');load()}return <main className="min-h-screen bg-[#F4F7F9] px-4 py-8"><div className="mx-auto max-w-5xl"><Link href="/admin" className="text-sm font-bold text-[#005EA8]">← Admin dashboard</Link><div className="mt-5 grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><form onSubmit={add} className="rounded-xl border bg-white p-6"><h1 className="text-xl font-extrabold text-[#12304A]">Create grant</h1><p className="mt-1 text-xs text-slate-500">Published grants appear on Find a Grant.</p><label className="mt-6 block text-sm font-semibold">Title<input required value={title} onChange={e=>setTitle(e.target.value)} className="mt-2 w-full rounded-lg border px-3 py-3 text-sm"/></label><label className="mt-4 block text-sm font-semibold">Category<input value={category} onChange={e=>setCategory(e.target.value)} className="mt-2 w-full rounded-lg border px-3 py-3 text-sm"/></label><label className="mt-4 block text-sm font-semibold">Description<textarea required value={description} onChange={e=>setDescription(e.target.value)} rows={5} className="mt-2 w-full rounded-lg border px-3 py-3 text-sm"/></label>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<button className="mt-5 rounded-lg bg-[#005EA8] px-5 py-3 text-sm font-bold text-white">Publish grant</button></form><section className="rounded-xl border bg-white"><div className="border-b p-6"><h2 className="font-bold">Published grants</h2></div><div className="divide-y">{grants.map(g=><div key={g.id} className="p-5"><div className="text-sm font-bold">{g.title}</div><div className="mt-1 text-xs text-[#005EA8]">{g.category} · {g.status}</div><p className="mt-2 text-sm text-slate-500">{g.description}</p></div>)}</div></section></div></div></main>}
